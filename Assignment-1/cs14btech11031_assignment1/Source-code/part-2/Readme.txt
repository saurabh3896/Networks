Part-2
------

1. Run the following command in terminal : make

2. In the same terminal window, run :
	./server [packet size]

3. In another terminal window, run :
	./client [interval] [packet size]

The code generates two files, containing the throughput and the average_delay, for graph plotting purposes.
